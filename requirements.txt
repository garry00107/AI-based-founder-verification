# In your requirements.txt, make sure you have:
Flask>=2.0
gunicorn
requests
beautifulsoup4
lxml
nltk
spacy>=3.0.0,<4.0.0 # Pin spaCy to avoid major breaking changes if possible
geopy
Flask-Caching
xhtml2pdf
python-dotenv # Good for local dev, not used by Render directly for secrets
# ... other dependencies like Werkzeug if explicitly listed ...
